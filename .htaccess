RewriteEngine On
RewriteBase /

# Redireciona se não for um arquivo ou diretório real
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Reescreve para index.php passando a URL como parâmetro
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]